/**
 * Identical to the canonical Separator (only formatting + a `"use client"`
 * directive differed). Re-exported to remove the duplicate file content
 * without touching every import path.
 */
export { Separator } from "@/components/ui/separator"
