node_modules
.next
out
.env
.env.local
.env.*.local
!.env.example

# User uploads — never commit
/public/uploads/**
!/public/uploads/.gitkeep

# Stray Office docs / scratch files at the repo root
/*.docx
/*.xlsx
/*.pdf

# Editor + OS noise
.DS_Store
*.swp
.idea
.vscode

# Build caches
tsconfig.tsbuildinfo

# Build identity stamp (written by prebuild, read by next.config.ts)
.build-id
